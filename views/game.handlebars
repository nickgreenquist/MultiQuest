<html lang="en">
<head>
    <script> 
      const username = `{{info.username}}`;
      const level = {{info.level}};
      const attack = {{info.attack}};
      const speed = {{info.speed}};
      const exp = {{info.exp}};
      const maxDistance = {{info.maxDistance}};
      const spellPower = {{info.spellPower}};
      const maxHealth = {{info.maxHealth}};
    </script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="https://npmcdn.com/babel-core@5.8.38/browser.min.js"></script>
    <script type="text/babel" src="/assets/game.js"></script>
</head>
<body>
    <canvas id="canvas" height="768" width="1366" style="background-image:url('/assets/img/background1.png');">Please use an HTML 5 browser</canvas>
</body>
</html>